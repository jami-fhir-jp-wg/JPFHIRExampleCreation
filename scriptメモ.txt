while read -r f;do outfname=`basename $f .fshegg`;echo "converting $f to ./input/fsh/$outfname.fsh";filepp -b -M/usr/local/share/filepp/modules -m bigdef.pm -m foreach.pm -m bigfunc.pm $f >./input/fsh/$outfname.fsh;done < <(find ./fshegg -name *.fshegg -mindepth 1);

sushi .
