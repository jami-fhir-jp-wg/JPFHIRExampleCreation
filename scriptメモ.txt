while read -r f;
do outfname=`basename $f .fshegg`;
  echo "converting $f to ./fsh/$outfname.fsh";
  filepp -b -M/usr/local/share/filepp/modules -m bigdef.pm $f >./fsh/$outfname.fsh;
done < <(find ./egg -name *.egg -mindepth 1);

sushi .
